---
# dbt_utils 1.3.0+ declara require-dbt-version compatível com o Fusion engine.
# Após bump, rode `dbt deps` (core) e `dbtf deps` (Fusion) para revalidar.
packages:
  - package: dbt-labs/dbt_utils
    version: 1.3.0
