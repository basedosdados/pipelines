---
description: Gather context for a Data Basis dataset onboarding (raw sources, docs, coverage, org)
argument-hint: <dataset_slug> [raw_data_path] [drive_folder_path]
---

Spawn the `context` agent with: $ARGUMENTS

The agent will search for official documentation, ask the user to confirm source details, and output a structured context block for downstream agents.
